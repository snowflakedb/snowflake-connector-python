name: Build and Test

on:
  push:
    branches:
      - master
      - main
    tags:
      - v*
  pull_request:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      logLevel:
        default: warning
        description: "Log level"
        required: true
      tags:
        description: "Test scenario tags"

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}

# 🧪 Simulate Azure Batch locally using Windows container
jobs:
  simulate-azure-batch:
    name: Simulate Azure Batch Job in Windows Container
    runs-on: windows-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t pyarrow-hw .

      - name: Run Docker container
        run: docker run --rm pyarrow-hw

# Disabled original jobs (for now)
# jobs:
#   lint:
#     ...
#   dependency:
#     ...
#   build:
#     ...
#   test:
#     ...
#   test-olddriver:
#     ...
#   test-noarrowextension:
#     ...
#   test-fips:
#     ...
#   test-lambda:
#     ...
#   combine-coverage:
#     ...
